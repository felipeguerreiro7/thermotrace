# Versões verificadas no PyPI em 22/08/2026.
# Fixadas com == de propósito: build reproduzível vale mais que estar na
# última versão. Atualizar é decisão consciente, não efeito colateral.

fastapi==0.141.1
uvicorn[standard]==0.52.4

sqlalchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4

pydantic==2.13.4
pydantic-settings==2.15.0
email-validator==2.2.0

# Segurança (usado a partir da Fase 4)
argon2-cffi==25.1.0
pyjwt==2.13.0

# Laudo em Excel — reaproveita tools/exportar_excel.py
xlsxwriter==3.2.9

# Logs estruturados
structlog==26.1.0

# Testes
pytest==9.1.1
pytest-asyncio==1.3.0
httpx==0.28.1
